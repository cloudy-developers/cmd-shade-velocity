group=smp.cloud
version=1.0
description=A Velocity plugin for hiding all necessary commands in chat from players. Permissions to view and use a command can be granted, for example, through LuckPerms.
org.gradle.configuration-cache=true
org.gradle.parallel=true
org.gradle.caching=true
